apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculator-frontend.fullname" . }}
  labels:
    {{- include "calculator-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "calculator-frontend.selectorLabels" . | nindent 4 }}
