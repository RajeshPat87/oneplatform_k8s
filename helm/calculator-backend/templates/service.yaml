apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculator-backend.fullname" . }}
  labels:
    {{- include "calculator-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "calculator-backend.selectorLabels" . | nindent 4 }}
